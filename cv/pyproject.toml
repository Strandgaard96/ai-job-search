[project]
name = "job-search-cv-builder"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0",
    "rendercv[full]>=2.8",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
